type AiModel {
  modelId: String!
  name: String!
  provider: String!
  description: String!
  supportsTemperature: Boolean!
  supportsReasoning: Boolean!
  supportsVerbosity: Boolean!
}

extend type Query {
  aiModels: [AiModel!]!
}
